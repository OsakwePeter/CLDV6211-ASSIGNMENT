EventEase — Noir Luxe UI Redesign
===================================
Drop these files into your existing EventEase project,
replacing the originals at the same folder paths.

Files changed:
  wwwroot/css/site.css              — Full redesign (dark theme + gold)
  Views/Shared/_Layout.cshtml       — New sidebar navigation layout
  Views/Home/Index.cshtml           — Dashboard homepage
  Views/Venue/Index.cshtml          — Venue list
  Views/Venue/Details.cshtml        — Venue detail card
  Views/Venue/Delete.cshtml         — Delete confirmation
  Views/Event/Index.cshtml          — Event list
  Views/Event/Details.cshtml        — Event detail card
  Views/Event/Delete.cshtml         — Delete confirmation
  Views/Booking/Index.cshtml        — Booking list
  Views/Booking/Details.cshtml      — Booking detail card
  Views/Booking/Delete.cshtml       — Delete confirmation

No C# changes required — all controllers and models stay the same.
